version: "3"

vars:
  VERSION:
    sh: git describe --tags --always --dirty 2>/dev/null || echo "dev"
  LDFLAGS: -ldflags "-s -w -X main.version={{.VERSION}}"

tasks:
  build:
    desc: Build the nimbopacks binary into bin/
    cmds:
      - go build {{.LDFLAGS}} -o bin/nimbopacks ./cmd/nimbopacks

  install:
    desc: go install nimbopacks into GOBIN
    cmds:
      - go install {{.LDFLAGS}} ./cmd/nimbopacks

  test:
    desc: Run all tests
    cmds:
      - go test ./...

  test-verbose:
    desc: Run all tests with verbose output
    cmds:
      - go test -v ./...

  test-cover:
    desc: Run tests and write coverage.html
    cmds:
      - go test -coverprofile=coverage.out ./...
      - go tool cover -html=coverage.out -o coverage.html

  fmt:
    desc: gofumpt all code
    cmds:
      - gofumpt -w -l .

  lint:
    desc: Lint the code with golangci-lint
    cmds:
      - golangci-lint run --tests --timeout 10m --fix --config ./.golangci.yaml ./...

  clean:
    desc: Remove build output and coverage artifacts
    cmds:
      - rm -rf bin/ coverage.out coverage.html

  hooks:
    desc: Install git hooks (commit-msg conventional commits validator)
    cmds:
      - cp scripts/commit-msg .git/hooks/commit-msg
      - chmod +x .git/hooks/commit-msg
      - echo "Git hooks installed."

  detect:
    desc: Quick detect on current directory
    cmds:
      - go run ./cmd/nimbopacks detect .
