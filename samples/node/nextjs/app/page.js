export default function Page() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem' }}>
      <h1>Hello from nimbopacks — node/nextjs sample</h1>
      <p>
        This is a Next.js app built with <code>output: &quot;standalone&quot;</code>{' '}
        and served by the bundled Node server.
      </p>
    </main>
  );
}
