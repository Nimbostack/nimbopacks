/** @type {import('next').NextConfig} */
const nextConfig = {
  // `standalone` produces a self-contained server.js + minimal node_modules
  // tree under .next/standalone — that is what the image runs.
  output: 'standalone',
};

module.exports = nextConfig;
